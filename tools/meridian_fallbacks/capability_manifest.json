{
  "schema_version": 1,
  "package": "meridian_fallbacks",
  "package_version": "0.1.0",
  "generated_at": "2026-08-05",
  "source_proposal": "1abedabe-2f82-40e5-a320-3b32d550cc40",
  "related_sprint_items": {
    "4ff6ff22-c6b0-4f0d-915a-5c534c4d8135": {
      "title": "FEAT: track local Meridian fallback tools as a versioned package with explicit capability contracts",
      "delivers": [
        "__init__.py",
        "safe_ooxml_writer.py",
        "safe_image_insert.py",
        "patch_manifest.py",
        "transactional_merge.py",
        "capability_manifest.json",
        "tests/"
      ],
      "status": "landed"
    },
    "d3374b0e": {
      "title": "output_provenance_gate.py",
      "delivers": ["output_provenance_gate.py"],
      "status": "planned",
      "note": "Implemented by a sibling sprint item in a parallel isolated worktree. Deliberately NOT imported by this package's __init__.py to avoid a merge conflict with that commit -- import it directly once it lands: `from tools.meridian_fallbacks import output_provenance_gate`."
    },
    "8419f55f": {
      "title": "docx_completion_gate.py",
      "delivers": ["docx_completion_gate.py"],
      "status": "planned",
      "note": "Implemented by a sibling sprint item in a parallel isolated worktree. Deliberately NOT imported by this package's __init__.py to avoid a merge conflict with that commit -- import it directly once it lands: `from tools.meridian_fallbacks import docx_completion_gate`."
    }
  },
  "purpose": "Local, dependency-light fallback tools for safe OOXML (.docx) editing when the meridian-docs MCP extension or a Word MCP tunnel slot is unavailable. Exists to satisfy Meridian's capability-manifest fallback_chain contract (AGENTS.md, 649e095f) as a documented, already-wired alternative -- never ad-hoc improvisation. Zero dependency on the Meridian server, its DB, any MCP tool, or Serena memories; source-of-truth is this tracked package plus durable PatchManifest pointers, per the originating proposal.",
  "dependencies": {
    "python": ">=3.11",
    "stdlib_only": true,
    "third_party": [],
    "note": "python-docx is intentionally NOT used or required -- it is not a pypi-dependency of this repo's pixi environment (see pixi.toml), and this package must not require adding one. All OOXML handling is stdlib zipfile + xml.etree.ElementTree."
  },
  "do_not": [
    "Do not edit the canonical thesis DOCX (or any other production document) directly with these tools outside of a reviewed PatchManifest + backed-up transaction.",
    "Do not treat this package as a replacement for meridian-docs' stable-paragraph-ID navigation, captions, citations, bibliography, or equations -- see docs/docx-mcp-parity.md for the parity matrix between the Word MCP slot and meridian-docs.",
    "Do not depend on Serena or editor memories for how these modules behave -- this file plus the tracked source under tools/meridian_fallbacks/ is the source of truth.",
    "Do not delete or replace the pre-existing thesis temp_scripts prototype copies until parity with this tracked package is proven -- this package supersedes them in intent, not (yet) by removal."
  ],
  "modules": {
    "__init__": {
      "file": "__init__.py",
      "sha256": "76f710e5001f9abe9fa4a30cb01705937eeaf6e94216c8d9879e6fa0e549d89a",
      "byte_size": 3869,
      "supported_operations": [
        "re-exports the public API of every module below",
        "PACKAGE_VERSION / SCHEMA_VERSION constants"
      ],
      "limitations": [
        "Deliberately does NOT import output_provenance_gate or docx_completion_gate (see related_sprint_items d3374b0e / 8419f55f) -- import those submodules directly once they land."
      ]
    },
    "safe_ooxml_writer": {
      "file": "safe_ooxml_writer.py",
      "sha256": "067973d3d52aae5a5eb0ef84424740ef4dc0c82109be68bc03ea90da1f3570cc",
      "byte_size": 13655,
      "supported_operations": [
        "read_parts_from_bytes(data) -> {part_name: bytes}",
        "build_zip_bytes(parts) -> bytes (deterministic, sorted-name order)",
        "verify_zip_bytes(data, required_parts=...) -> DocxValidationReport (real zip CRC check, required-part presence, well-formed-XML check on word/document.xml)",
        "SafeOoxmlWriter.read_parts() / write_parts() / restore_backup() -- validate-then-backup-then-atomic-replace discipline (temp file in the same directory + fsync + os.replace)",
        "compute_sha256(data) -> hex digest"
      ],
      "limitations": [
        "Validates zip integrity, required-part presence, and word/document.xml well-formedness only -- this is NOT full OOXML schema validation.",
        "Atomic replace relies on os.replace within the SAME directory/filesystem as the target -- a target directory that isn't a real filesystem path (e.g. some virtual/network mounts) is unsupported.",
        "Backups accumulate on disk (one timestamped file per write) -- this module does not prune old backups; callers that write frequently should manage retention themselves."
      ]
    },
    "safe_image_insert": {
      "file": "safe_image_insert.py",
      "sha256": "3e6d97323171d1f53d308adaf351b3453aa2ec3436fcd2f03e65c3097b38b503",
      "byte_size": 14663,
      "supported_operations": [
        "compute_image_insert_parts(parts, image_bytes, image_ext=..., anchor_text=..., paragraph_index=..., width_emu=..., height_emu=...) -> ImageInsertResult (pure, in-memory)",
        "insert_image(docx_path, image_path=... | image_bytes=..., ...) -> ImageInsertWriteResult (disk-touching, uses SafeOoxmlWriter)",
        "build_drawing_xml(...) -> minimal valid inline <w:drawing> OOXML",
        "supported image extensions: png, jpg/jpeg, gif, bmp, tif/tiff, emf, wmf"
      ],
      "limitations": [
        "Never decodes or validates actual image content/dimensions -- treats image bytes as opaque, keyed only by file extension for the Content-Type mapping.",
        "Locates insertion anchors via a targeted regex over word/document.xml's raw text (paragraph boundaries via `<w:p>`/`<w:p/>` splicing) rather than a full XML tree edit -- see safe_ooxml_writer.py's module docstring for why. Assumes document.xml has no literal `</w:p>`-shaped text inside a run (true for every real Word/python-docx-produced document; not schema-impossible in adversarial input).",
        "anchor_text matching is a plain substring match against a paragraph's concatenated, tag-stripped text -- not a cross-run-aware phrase search and not case-insensitive.",
        "Always inserts an INLINE drawing (wp:inline) -- floating/anchored (wp:anchor) image placement is not supported."
      ]
    },
    "patch_manifest": {
      "file": "patch_manifest.py",
      "sha256": "cc251a6e40e3f5dcd64ceb56724a159bc1e7d77d4588886bc330f10ec72dfd03",
      "byte_size": 11157,
      "supported_operations": [
        "PatchManifest.create(...) / create_from_file(...) -- new draft manifest, optionally hashing the current on-disk target as base_sha256",
        "PatchManifest.add_operation(kind, target_part, description, payload=..., metadata=...) -- kind in {replace_part, insert_image, custom}",
        "PatchManifest.verify_base_unchanged(...) -- staleness check against the target's current hash",
        "PatchManifest.to_dict()/to_json()/from_dict()/from_json() -- schema_version-checked (de)serialization",
        "PatchManifest.save(path)/load(path) -- atomic JSON persistence (temp file + fsync + os.replace)",
        "mark_applied()/mark_aborted(reason) -- terminal status transitions; only a 'draft' manifest accepts add_operation"
      ],
      "limitations": [
        "Never embeds large binary payloads inline -- only a sha256 + byte size per operation. The real payload bytes must be supplied separately at apply time and are verified against the recorded hash.",
        "from_dict()/from_json() reject any manifest whose schema_version does not exactly match PATCH_MANIFEST_SCHEMA_VERSION (currently 1) -- no automatic migration between schema versions."
      ]
    },
    "transactional_merge": {
      "file": "transactional_merge.py",
      "sha256": "06307158ffb3bd1b3e62d4fb019c5cb522d2b196de4605aeeb52435cf17f744f",
      "byte_size": 12298,
      "supported_operations": [
        "apply_patch_manifest(manifest, payloads=..., allow_stale_base=False, dry_run=False, appliers=...) -> MergeResult -- all-or-nothing apply of every operation in a manifest",
        "rollback(manifest, merge_result) -> WriteResult -- restore the target to its pre-apply state using the backup a successful apply created",
        "DEFAULT_APPLIERS: {'replace_part': ..., 'insert_image': ...} -- extensible via the appliers= kwarg for custom operation kinds"
      ],
      "limitations": [
        "dry_run=True is a pure preview: it never mutates manifest.status (success or failure), so a failed preview can be fixed and re-previewed without rebuilding the manifest. Only a real (non-dry-run) apply transitions a manifest out of 'draft'.",
        "A stale base_sha256 raises MergeConflictError rather than auto-merging -- there is no 3-way merge; the caller must explicitly opt in via allow_stale_base=True or re-create the manifest against the current file.",
        "Operations are applied strictly in the order they appear in manifest.operations -- no dependency graph or reordering."
      ]
    }
  },
  "fallback_chain_example": {
    "comment": "Illustrative set_capability_manifest() entry showing how a project would register this package as the fallback for a docx-editing capability whose primary tool is the meridian-docs MCP extension or a Word MCP tunnel slot.",
    "capability_id": "docx_editing",
    "purpose": "Edit .docx files (insert content, insert images, apply reviewed patches) when authoring a document deliverable.",
    "required_tools": ["meridian-docs:write_section", "meridian-docs:insert_image"],
    "fallback_chain": [
      "tools/meridian_fallbacks:transactional_merge.apply_patch_manifest",
      "tools/meridian_fallbacks:safe_image_insert.insert_image",
      "tools/meridian_fallbacks:safe_ooxml_writer.SafeOoxmlWriter"
    ],
    "availability_policy": "degraded_ok",
    "verification_command": "pixi run python -m pytest tools/meridian_fallbacks/tests/ -q --timeout=60 -p no:xdist"
  },
  "implementation_notes": "Copy/paste-ready usage:\n\nfrom tools.meridian_fallbacks import (\n    PatchManifest, apply_patch_manifest, insert_image, SafeOoxmlWriter,\n)\n\n# 1) Simple, single-shot edit (no manifest needed):\ninsert_image(\n    \"report.docx\", image_path=\"chart.png\",\n    anchor_text=\"Figure 1\", width_emu=4_572_000, height_emu=2_286_000,\n)\n\n# 2) Reviewable, transactional multi-op edit:\nmanifest = PatchManifest.create_from_file(\"report.docx\")\nimage_bytes = open(\"chart.png\", \"rb\").read()\nop = manifest.add_operation(\n    \"insert_image\", \"word/document.xml\", \"insert Figure 1 chart\",\n    payload=image_bytes, metadata={\"anchor_text\": \"Figure 1\", \"image_ext\": \"png\"},\n)\nmanifest.save(\"report.patch_manifest.json\")  # durable, reviewable pointer\n\n# ... later, possibly in a different process/session ...\nmanifest = PatchManifest.load(\"report.patch_manifest.json\")\nresult = apply_patch_manifest(manifest, payloads={op.op_id: image_bytes})\nassert result.success, result.error\n# result.backup_path points at the pre-apply snapshot if a rollback is needed.\n",
  "notes": "Record maintained by hand alongside the tracked source under tools/meridian_fallbacks/. Regenerate the sha256/byte_size fields (see the module docstrings) after any edit to the corresponding .py file so this manifest never drifts from what is actually committed."
}
