# Meridian — Start Here

> This is a Meridian-coordinated project. Read this first, then load the full context below.

## For Claude Code (executor)

Paste this to start any session:

```
Read data/meridian-build_handoff.md then start_session(project_id="5787cc92-ba7d-4788-b17c-28ab7938b839", session_name="<your-session-name>", human_id="adam", client="claude_code")
```

The full handoff (`data/meridian-build_handoff.md`) contains:
- L0: North star + pinned decisions (always read)
- L1: Current sprint + recent tasks (read to resume)
- L2: Full history (skim if space allows)

## For Planning Chat (claude.ai)

Paste this into a new chat tab:

```
I'm working on Meridian (usemeridian.us) — an open-source MCP coordination layer for AI coding sessions.
Call generate_handoff(project_id="5787cc92-ba7d-4788-b17c-28ab7938b839", mode="planner") to load current context.
```

## Quick Commands

```bash
pixi run start              # local server at localhost:7878/dashboard
pixi run test               # test suite (target: 545+)
pixi run launch             # launch morning (unsets SITE_PASSWORD + health checks)
pixi run install-companions # install repomix globally via npm (one-time)
```

## Companion MCP Config (all pixi-managed)

```json
{
  "mcpServers": {
    "meridian":     { "command": "pixi", "args": ["run", "mcp"],             "cwd": "/path/to/Meridian" },
    "text-editor":  { "command": "pixi", "args": ["run", "text-editor-mcp"], "cwd": "/path/to/Meridian" },
    "repomix":      { "command": "pixi", "args": ["run", "repomix-mcp"],     "cwd": "/path/to/Meridian" }
  }
}
```

Run `pixi install` once — mcp-text-editor is included. Run `pixi run install-companions` once for repomix.

---
*Generated by Meridian · Project: meridian-build (5787cc92)*
